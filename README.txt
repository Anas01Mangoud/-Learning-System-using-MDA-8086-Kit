This repository contains assembly code that runs on an embedded system, controlling a display and handling input from a keypad. The system is designed to perform actions based on keypad inputs and display specific messages on the screen, including characters and numbers. It interacts with multiple ports and registers, controls a seven-segment display, and manipulates LED matrix data. The main functionalities include scanning input, displaying messages, and controlling a timer for various tasks.